---
title: Theme
description: Customize colors, fonts, and design variables of Litefy UI.
---

Copy the following CSS variables into your global stylesheet:

<Source type="css" name="theme" />

This theme includes both light and dark color schemes. The `.dark` class will automatically apply the dark mode when present.

> Note: The variables use OKLCH color values, which are supported by all modern browsers. If you want to use them in Tailwind class names, they have been mapped via `@theme inline`.

## Theme Composition

The Litefy theme consists of two freely combinable modules:

- **Surface**: the neutral canvas palette that controls page background and text colors.
- **Brand**: the interactive brand palette that controls buttons and accent controls.

> To switch palettes at runtime with `useTheme`, see [`useTheme`](./utils/use-theme.mdx).

## Surface Presets

Each preset overrides the neutral variables (`--background`, `--foreground`, `--muted`, `--muted-foreground`, `--neutral`).

<PresetTabs
  items={[
    "gray-surface",
    "mauve-surface",
    "mist-surface",
    "olive-surface",
    "slate-surface",
    "stone-surface",
    "taupe-surface",
    "zinc-surface",
  ]}
/>

## Brand Presets

Each preset overrides the brand variables (`--primary` and `--accent`).

<PresetTabs
  items={[
    "amber-brand",
    "cyan-brand",
    "emerald-brand",
    "fuchsia-brand",
    "indigo-brand",
    "lime-brand",
    "orange-brand",
    "pink-brand",
    "purple-brand",
    "rose-brand",
    "sky-brand",
    "teal-brand",
    "violet-brand",
  ]}
/>
