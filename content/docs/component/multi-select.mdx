---
title: MultiSelect
description: A multi-select component for selecting multiple options from a list
---

## Installation

<Tabs items={['CLI', 'Manual']} >

<Tab value="CLI">

<PackageManagerTabs command="litefy add muti-select" />

</Tab>

<Tab value="Manual">

<SourceCode component="muti-select" />

</Tab>

</Tabs>

## Usage

### Basic Usage

A simple multi-select with a list of options.

<ComponentPreview 
  name="multi-select-basic"
/>

### Grouped Options

Organize options into logical groups for better user experience.

<ComponentPreview 
  name="multi-select-grouped"
/>

### Controlled Mode

Manage selected values externally with controlled state.

<ComponentPreview 
  name="multi-select-controlled"
/>

## API Reference

### MultiSelect

A multi-select component that allows selecting multiple options.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `(SelectOption \| SelectOptionGroup)[]` | - | List of options or option groups |
| `value` | `string[]` | - | Controlled selected values |
| `defaultValue` | `string[]` | `[]` | Uncontrolled default selected values |
| `onChange` | `(values: string[]) => void` | - | Callback when selection changes |
| `placeholder` | `string` | - | Placeholder text |
| `invalid` | `boolean` | `false` | Whether the select is in invalid state |
| `className` | `ClassNameValue` | - | Custom CSS class. Supports strings, arrays, objects, booleans, and more. |

### SelectOption

| Prop | Type | Description |
|------|------|-------------|
| `label` | `string` | Display label for the option |
| `value` | `string` | Unique value for the option |

### SelectOptionGroup

| Prop | Type | Description |
|------|------|-------------|
| `group` | `string` | Group label |
| `options` | `SelectOption[]` | Options in this group |
