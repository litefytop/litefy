---
title: Cascader 级联选择器
description: 级联选择器，显示面包屑样式的触发器并通过 CSS 锚点定位显示面板覆盖层
---

## 安装

<Tabs items={['CLI', '手动']} >

<Tab value="CLI">

<PackageManagerTabs command="litefy@latest add cascader" />

</Tab>

<Tab value="手动">

<Source type="component" name="cascader" />

</Tab>

</Tabs>

## 用法

### 基础用法

传递树形结构给 `tree`，点击某一级会在选择框下方打开对应的面板，点击叶子节点则关闭面板并完成选择。

<ComponentPreview
  name="cascader-basic"
/>

```tsx
import { Cascader, type CascaderNode } from "@/ui";

const tree: CascaderNode[] = [
  {
    value: "california",
    label: "California",
    children: [
      {
        value: "los-angeles",
        label: "Los Angeles",
        children: [
          { value: "downtown", label: "Downtown" },
          { value: "hollywood", label: "Hollywood" },
          { value: "pasadena", label: "Pasadena" },
        ],
      },
      {
        value: "san-francisco",
        label: "San Francisco",
        children: [
          { value: "soma", label: "SoMa" },
          { value: "richmond", label: "Richmond" },
          { value: "sunset", label: "Sunset" },
        ],
      },
    ],
  },
  {
    value: "new-york",
    label: "New York",
    children: [
      {
        value: "new-york-city",
        label: "New York City",
        children: [
          { value: "manhattan", label: "Manhattan" },
          { value: "brooklyn", label: "Brooklyn" },
        ],
      },
    ],
  },
];

export default function Demo() {
  return <Cascader tree={tree} />;
}
```

## 注意

- 触发器样式像面包屑，但使用普通按钮配合 `ChevronRight` 而不是 Breadcrumb 组件，因为面包屑的语义用于页面导航。
- 面板内联渲染并声明 `popover="manual"`（top layer 不受祖先 `overflow` 裁剪），通过 CSS 锚点定位锚定到对应的触发器按钮。
- 需要 Chrome 125+ 以支持 CSS 锚点定位。

## API 参考

### Cascader

级联选择器组件，管理选择状态并渲染面包屑样式的触发器与面板覆盖层。

| 属性  | 类型             | 默认值 | 描述                          |
| :---- | :--------------- | :----- | :--------------------------- |
| `tree` | `CascaderNode[]` | -      | 级联选择的树形结构            |

### CascaderNode

级联选择器树节点的数据结构。

| 字段     | 类型              | 默认值 | 描述                      |
| :-------- | :---------------- | :----- | :----------------------- |
| `value`   | `string`          | -      | 节点的唯一值              |
| `label`   | `string`          | -      | 节点的显示文本            |
| `children` | `CascaderNode[]` | -      | 下一级的子节点数组        |
