---
title: Button 按钮
description: 支持多种变体和加载状态的按钮组件
---

## 安装

<Tabs items={['CLI', 'Manual']} >

<Tab value="CLI">

<PackageManagerTabs command="litefy@latest add button" />

</Tab>

<Tab value="Manual">

<Source type="component" name="button" />

</Tab>

</Tabs>

## 用法

### 基础变体

不同的按钮变体适用于各种场景。

<ComponentPreview 
  name="button-basic"
/>

### 加载状态

带有加载指示器和禁用状态的按钮。

<ComponentPreview 
  name="button-loading"
/>

### 带图标

支持图标的按钮，增强视觉反馈。

<ComponentPreview 
  name="button-icon"
/>

## API 参考

### Button

按钮组件。

| Prop            | Type                                                | Default     | Description                                               |
| --------------- | --------------------------------------------------- | ----------- | --------------------------------------------------------- |
| `variant`       | `'primary' \| 'danger' \| 'outline' \| 'text'` | `'primary'` | 按钮视觉变体                                              |
| `className`     | `ClassNameValue`                                    | -           | 自定义 CSS 类，接受字符串、数组以及所有假值（false、null、undefined、0、0n）；不支持 true 和对象。 |
| `loadingConfig` | `ButtonLoadingConfig`                               | -           | 加载状态配置                                              |
| `...props`      | `React.ComponentProps<"button">`                    | -           | 支持全部 button 原生属性（除 `className`），如 `type`、`disabled`                                                  |

### ButtonLoadingConfig

| Prop      | Type              | Default | Description          |
| --------- | ----------------- | ------- | -------------------- |
| `loading` | `boolean`         | `false` | 按钮是否处于加载状态 |
| `icon`    | `React.ReactNode` | -       | 自定义加载图标       |
