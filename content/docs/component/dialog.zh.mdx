---
title: Dialog 对话框
description: 使用原生 HTML dialog 元素构建的模态对话框组件
---

## 安装

<Installation name="dialog" />

## 用法

### 基础用法

受控模式的基础对话框组件，内置 Tab 焦点圈定与 Esc 关闭。

<ComponentPreview 
  name="dialog-basic"
/>

### 自定义

使用可组合零件自行实现开关生命周期。焦点圈定逻辑在组合组件 `Dialog` 内，不属于原子组件。

<ComponentPreview 
  name="dialog-custom"
/>

### 命令对话框

通过 `dialog.success` / `dialog.error` / `dialog.warning` / `dialog.info` 命令式方法触发的对话框，动态创建并在关闭后自动销毁。

<ComponentPreview 
  name="dialog-command"
/>

## API 参考

### 高阶组件

开箱即用的成品组件。

#### Dialog

基于原生 HTML `dialog` 元素的模态对话框组件，内置焦点圈定与 Esc 关闭处理。

| Prop              | Type                                                | Default   | Description                                                                   |
| ----------------- | --------------------------------------------------- | --------- | ----------------------------------------------------------------------------- |
| `open`            | `boolean`                                           | -（必填） | 受控打开状态，内部通过原生 `showModal()` / `close()` 同步                     |
| `onOpenChange`    | `(open: boolean) => void`                           | -         | 打开状态变化回调，Esc、关闭按钮点击、原生 `cancel` / `close` 时触发           |
| `onBackdropClick` | `(e: React.MouseEvent<HTMLDialogElement>) => void`  | -         | 点击遮罩时触发的回调，可实现点击遮罩关闭                                      |
| `classNames`      | `{ root?, content?, close? }`                       | -         | 各部分自定义类名，分别作用于 dialog 元素、内容区、关闭按钮                    |
| `styles`          | `{ root?, content?, close? }`                       | -         | 各部分内联样式                                                                |
| `children`        | `React.ReactNode`                                   | -         | 对话框内容，渲染在内容区 `DialogContent` 内                                   |
| `...props`        | `React.ComponentProps<"div">`                       | -         | 其余属性透传给内容区 `DialogContent`（`className` / `styles` 除外）           |

---

### 可组合组件

#### DialogRoot

dialog 元素，渲染为无内置样式与行为的原生 `dialog`。

| Prop        | Type                             | Default | Description                                  |
| ----------- | -------------------------------- | ------- | -------------------------------------------- |
| `className` | `ClassNameValue`                 | -       | 自定义类名，作用于原生 dialog 元素           |
| `...props`  | `React.ComponentProps<"dialog">` | -       | 支持全部 dialog 原生属性（除 `className`）   |

#### DialogClose

关闭按钮，内置样式，绝对定位于最近的定位祖先（即 dialog 元素）的右上角。

| Prop        | Type                             | Default | Description                                |
| ----------- | -------------------------------- | ------- | ------------------------------------------ |
| `className` | `ClassNameValue`                 | -       | 自定义类名                                 |
| `children`  | `React.ReactNode`                | -       | 按钮内容                                   |
| `...props`  | `React.ComponentProps<"button">` | -       | 支持全部 button 原生属性（除 `className`） |

#### DialogContent

内容区容器，内置面板默认样式：居中定位、`min-w-70 max-w-md` 宽度、圆角边框、阴影与背景。

| Prop        | Type                           | Default | Description                              |
| ----------- | ------------------------------ | ------- | ---------------------------------------- |
| `className` | `ClassNameValue`               | -       | 自定义类名                               |
| `children`  | `React.ReactNode`              | -       | 内容区内容                               |
| `...props`  | `React.ComponentProps<"div">`  | -       | 支持全部 div 原生属性（除 `className`）  |

#### dialog

命令式触发方法，动态创建对话框并在关闭后自动销毁。

| Method    | Type                                   | Description    |
| --------- | -------------------------------------- | -------------- |
| `success` | `(opts: DialogCommandOptions) => void` | 打开命令对话框 |
| `error`   | `(opts: DialogCommandOptions) => void` | 打开命令对话框 |
| `warning` | `(opts: DialogCommandOptions) => void` | 打开命令对话框 |
| `info`    | `(opts: DialogCommandOptions) => void` | 打开命令对话框 |

> 四个方法为语义化快捷方式，行为一致。
> 浏览器限制：模态 dialog（`showModal()`）同一时间只能打开一个，重复打开会被浏览器拦截，而非排队等待。

#### DialogCommandOptions

| Field      | Type              | Description          |
| ---------- | ----------------- | -------------------- |
| `title`    | `React.ReactNode` | 对话框标题           |
| `children` | `React.ReactNode` | 对话框内容           |
| `props`    | `DialogProps`     | 透传给 Dialog 的属性 |
