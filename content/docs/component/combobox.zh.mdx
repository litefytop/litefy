---
title: Combobox 组合框
description: 支持自动补全的选项选择组件
---

## 安装

<Tabs items={['CLI', 'Manual']} >

<Tab value="CLI">

<PackageManagerTabs command="litefy@latest add combobox" />

</Tab>

<Tab value="Manual">

<Source type="component" name="combobox" />

</Tab>

</Tabs>

## 用法

### 基础用法

基础组合框，使用静态选项。

<ComponentPreview 
  name="combobox-basic"
/>

### 异步搜索

支持异步选项获取和滚动分页加载的组合框，通过 `remote` 对象配置。

<ComponentPreview 
  name="combobox-async"
/>

### 自定义

使用可组合组件拼装，通过 CSS 锚点定位、键盘导航与空数据状态实现。

<ComponentPreview 
  name="combobox-custom"
/>

## API 参考

### 高阶组件

开箱即用的成品组件。

#### Combobox

支持静态选项与异步搜索的组合框，内置键盘导航与滚动分页加载。

| 属性            | 类型                          | 默认值                  | 描述                                                    |
| --------------- | ----------------------------- | ----------------------- | ------------------------------------------------------- |
| `value`         | `string`                      | -                       | 受控输入值                                              |
| `defaultValue`  | `string`                      | `""`                    | 非受控模式初始默认值                                    |
| `onValueChange` | `(value: string) => void`     | -                       | 输入框内容变化回调；受控模式下需在此更新 `value`       |
| `onSelect`      | `(option: string) => void`    | -                       | 下拉选项被选中时回调                                    |
| `options`       | `string[]`                    | -                       | 静态选项数组                                            |
| `remote`        | `ComboboxRemoteApi`           | -                       | 异步搜索配置对象，用于后端搜索、滚动分页                |
| `empty`         | `React.ReactNode`             | -                       | 下拉无数据时自定义渲染内容                              |
| `classNames`    | `{ root?: ClassNameValue; popover?: ClassNameValue; list?: ClassNameValue; option?: ClassNameValue }` | - | 内部元素的自定义类名 |
| `styles`        | `{ root?: React.CSSProperties; popover?: React.CSSProperties; list?: React.CSSProperties; option?: React.CSSProperties }` | - | 内部元素的内联样式；`styles.popover` 会与内置锚点定位合并 |
| `...props`      | `React.ComponentProps<"input">` | -                     | 支持全部 input 原生属性（除 `className`、`style`、`onChange`、`onSelect`），如 `placeholder`、`disabled`、`ref` |

#### ComboboxRemoteApi

| Field         | Type                       | Description            |
| ------------- | -------------------------- | ---------------------- |
| `data`        | `string[]`                 | 当前返回的选项列表     |
| `loading`     | `boolean`                  | 是否加载中             |
| `isSearching` | `boolean`                  | 是否正在搜索           |
| `hasMore`     | `boolean`                  | 是否还有更多分页数据   |
| `search`      | `(keyword:string) => void` | 输入关键词触发搜索     |
| `loadMore`    | `() => void`               | 滚动到底部触发加载更多 |

---

### 可组合组件

#### ComboboxRoot

外层容器，为下拉浮层提供定位锚点。

| Prop        | Type                          | Default | Description                             |
| ----------- | ----------------------------- | ------- | --------------------------------------- |
| `className` | `ClassNameValue`              | -       | 自定义类名                              |
| `...props`  | `React.ComponentProps<"div">` | -       | 支持全部 div 原生属性（除 `className`） |

#### ComboboxInput

搜索输入框，内置尺寸、边框与背景样式。

| Prop           | Type                            | Default | Description                                           |
| -------------- | ------------------------------- | ------- | ----------------------------------------------------- |
| `value`        | `string`                        | -       | 受控输入值                                            |
| `defaultValue` | `string`                        | -       | 非受控模式初始值                                      |
| `placeholder`  | `string`                        | `"typing to search..."` | 输入框占位符                          |
| `className`    | `ClassNameValue`                | -       | 自定义类名                                            |
| `...props`     | `React.ComponentProps<"input">` | -       | 支持全部 input 原生属性（除 `className`、`onSelect`） |

#### ComboboxContent

下拉浮层容器，基于原生 Popover API 渲染为 `popover="manual"` 的 div

| Prop        | Type                          | Default | Description                             |
| ----------- | ----------------------------- | ------- | --------------------------------------- |
| `className` | `ClassNameValue`              | -       | 自定义类名                              |
| `...props`  | `React.ComponentProps<"div">` | -       | 支持全部 div 原生属性（除 `className`） |

#### ComboboxList

下拉列表，渲染为 `role="listbox"` 的 ul。滚动接近底部约 `120px` 时自动触发 `onScrollBottom`

| Prop             | Type                         | Default | Description                            |
| ---------------- | ---------------------------- | ------- | -------------------------------------- |
| `onScrollBottom` | `() => void`                 | -       | 滚动接近底部时的回调                   |
| `empty`          | `React.ReactNode`            | -       | `children` 为空时渲染的内容            |
| `className`      | `ClassNameValue`             | -       | 自定义类名                             |
| `...props`       | `React.ComponentProps<"ul">` | -       | 支持全部 ul 原生属性（除 `className`） |

#### ComboboxOption

单个选项，渲染为 `role="option"` 的 li，`aria-selected` 样式自动应用

| Prop        | Type                         | Default | Description                                |
| ----------- | ---------------------------- | ------- | ------------------------------------------ |
| `id`        | `string`                     | -       | 选项 id，用于 `aria-activedescendant` 关联 |
| `className` | `ClassNameValue`             | -       | 自定义类名                                 |
| `...props`  | `React.ComponentProps<"li">` | -       | 支持全部 li 原生属性（除 `className`）     |
