---
title: 键盘按键
description: 用于渲染键盘快捷键和单个按键标签的键盘按键样式。
---

## 源码

<Source type="css" name="kbd" />

## 示例

### 基础用法

在 `<kbd>` 元素上使用 `.kbd` 类来渲染单个按键。

<ComponentPreview name="kbd-basic" />

### 组合按键

组合多个 `.kbd` 元素并配合分隔符，用于展示键盘快捷键。

<ComponentPreview name="kbd-combination" />
