# 进行中：root 样式约定全库对齐（暂停于 2026-09-06，待续）

## 约定（已定案，无争议）
1. 每个成品的 root = 渲染树最外层元素（Collapse 的 root 是 CollapseRoot，不是 trigger/content）。
2. `className` / `style` 永远作用于 root。
3. `classNames` / `styles` 只列 root 之外的零件，**禁止 root 键**。
4. 片段型组件（Popover/Select/DatePicker，trigger+panel 兄弟渲染）无 className，只有 classNames。
5. 零件本身是 root 时（如 WatermarkRoot），className/style 合并到它，classNames 不含该键。

## 已完成（源码，typecheck 当前 0 错误）
avatar / breadcrumb(见遗留#1) / dialog / wizard / slider / transfer / drawer / scroll-shadow /
toast(item root 定制移除，Toaster 容器 className 已接线) / upload / watermark / banner(viewport 键删) /
checkbox(新增 className/style) / image(同 avatar 处理) / form(FormField/Fieldset) / form-item /
list(root 键删，className 直用) / input(group 键删，className→InputGroup) / password / number-field /
picker(root 键删，className→PickerRoot) / select(trigger 键删，className→trigger)
timeline 本来就合规；masonry 本来就合规；popover/date-picker 片段型合规。

## 待办（今晚继续）
1. **breadcrumb.tsx 未完成**：BreadddcrumbProps 还没加 `className?: ClassNameValue`（Omit 掉了），
   需加 prop + 解构 + `<BreadcrumbRoot {...props} className={cn(className)} style={style}>`。
2. **demo 迁移核查**：collapse/image/preview-card/scroll-shadow 的 root→className 已改；
   全库 grep `classNames={{ root` 与 `classNames?.root` 应为零。
3. **文档表格（EN+ZH，未动）**：以下组件的 classNames/styles 表需要按新形状改写
   （删 root 行、加 className/style 行；Input 系把 group 行换成 className 行）：
   avatar, breadcrumb, collapse, watermark, dialog, drawer, slider, toast, transfer, wizard,
   scroll-shadow, upload, banner(删 viewport 行), image, checkbox(加 className 行), timeline(核对),
   form(FormField/Fieldset 行), list, input, password, number-field, picker, select(trigger 行→className)。
   同时 grep 文档正文/示例代码里的旧键用法。
4. form.mdx / form.zh.mdx 第 78 行"Invalid Control Styling"提到包装器列表，确认无需再改。
5. 验证：typecheck（当前已过）→ build → 浏览器抽查（select 面板、input 聚焦、collapse）。
6. 完成后删除本进度文件。

## 环境状态
- dev server 5173（后台 pnpm dev）；生产预览 4173 已被杀，需要时 `npx serve build/client -s -l 4173`。
- 注意：后台标签页会吞真实键盘事件/IO 回调（IAB 节流），交互验证优先用生产构建 + 前台标签页，
  或合成事件派发。
- 本轮相关的破坏性变更：NumberGroup/NumberDecrement/NumberIncrement、SelectGroup/SelectRoot/SelectIcon
  零件已删除（NumberField/Select 已重设计为键盘步进+popover 版本）。
